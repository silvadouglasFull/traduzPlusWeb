export type HomeSections = {
    aboutSection: string
    whaWeoffer: string
    greatExpertise: string
    faq: string
    flashyBanner: string
    contact: string
}
export type FooterSections = {
    links: string
}
export type Sections = {
    home: HomeSections
    footer: FooterSections
}