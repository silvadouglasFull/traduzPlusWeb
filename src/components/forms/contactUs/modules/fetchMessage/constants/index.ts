import type { EndPoints, Prefix } from "@components/forms/contactUs/modules/fetchMessage/constants/types";
export const prefix: Prefix = 'emails'
export const endPoints: EndPoints = {
    sendMessage: '/send'
}