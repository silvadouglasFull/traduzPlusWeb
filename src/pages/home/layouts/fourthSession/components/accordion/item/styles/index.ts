import { colors } from "@flavor/constants/colors"
import type { Styles } from "@pagesHome/layouts/fourthSession/components/accordion/item/types"
export const styles: Styles = {
    color: {
        color: colors.OxfordBlue
    }
}