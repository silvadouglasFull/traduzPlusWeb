/**
 * IMain.ts
 * @description Interface for the main operation with a boolean return value.
 */

/**
 * Contract for the main operation service.
 */
export interface IMain {
    /**
     * Executes the main operation and returns a boolean result.
     * @returns {boolean}
     */
    execute(): boolean;
}
